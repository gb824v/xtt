<!-- 
BannerAdvertisement.Trans.Server.TC.004  - Start Web-Server to Invoke the same xmp-clickthrough URL multiple times after the relevant xmp-image URL is invoked during Transparent Proxy Condition.

Test Description:
 1. Start Web Server.
 2. Wait for get request.
 3. Stop Web Server.

Expected Result:
 -  XMP should invoke the same xmp-clickthrough URL multiple times when relevant xmp-image URL is invoked.

XTT Commands:

 1.  - startWebServer
 2.  - waitForRequests
 3.  - stopWebServer
-->

<test>

	<function name="testName" module="Basic">
		<parameter>BannerAdvertisement.Trans.Server.TC.004</parameter>
	</function>

	<description>Start Web-Server to Invoke the same xmp-clickthrough URL multiple times after the relevant xmp-image URL is invoked during Transparent Proxy Condition.</description>

	<function name="startWebServer" module="HTTP"/>

	<function name="waitForRequests" module="HTTP">
		<parameter>1</parameter>
	</function>

	<function name="stopWebServer" module="HTTP"/>

</test>
