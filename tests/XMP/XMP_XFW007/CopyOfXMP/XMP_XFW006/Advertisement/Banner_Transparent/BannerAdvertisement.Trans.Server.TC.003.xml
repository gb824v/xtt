<!-- 
BannerAdvertisement.Trans.Server.TC.003  - Start Web-Server to Invoke the same xmp-image URL multiple times during Transparent Proxy Condition.

Test Description:
 1. Start Web Server.
 2. Wait for get request.
 3. Stop Web Server.

Expected Result:
 -  XMP should invoke the xmp-image URL multiple times with different advert images.

XTT Commands:

 1.  - startWebServer
 2.  - waitForRequests
 3.  - stopWebServer
-->

<test>

	<function name="testName" module="Basic">
		<parameter>BannerAdvertisement.Trans.Server.TC.003</parameter>
	</function>

	<description>Start Web-Server to Invoke the same xmp-image URL multiple times during Transparent Proxy Condition.</description>

	<function name="startWebServer" module="HTTP"/>

	<function name="waitForRequests" module="HTTP">
		<parameter>1</parameter>
	</function>

	<function name="stopWebServer" module="HTTP"/>

</test>
